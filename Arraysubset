class Compute {
    public String isSubset( long a1[], long a2[], long n, long m)
    {
        int count=0;
        for(int i=0;i<n;i++)
        {
            for(int j=0;j<m;j++)
            {
                
                if(a1[i]==a2[j])
                {
                    count++;
                }
            }
        }

        
        if(count==m)
        {
            return "Yes";
        }
        else
        {
            return "No";
        }
        
    }
}
